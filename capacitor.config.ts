import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ng-multistep-forms',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
